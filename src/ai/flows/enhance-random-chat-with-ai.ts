export async function enhanceRandomChat(_: { currentUserProfile?: string; otherUserProfile?: string }) {
  // Dev stub: return a simple decision object
  return { iceCandidateDecision: 'No issues detected' };
}
